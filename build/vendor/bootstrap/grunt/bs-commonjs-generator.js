"use strict";
var fs = require("fs"),
    path = require("path"),
    COMMONJS_BANNER = "// This file is autogenerated via the `commonjs` Grunt task. You can require() this file in a CommonJS environment.\n";module.exports = function (e, r, i) {
  function t(e) {
    var r = path.relative(a, e).replace(/\\/g, "/");return "require('" + r + "')";
  }var a = path.dirname(i),
      n = COMMONJS_BANNER + r.map(t).join("\n");try {
    fs.writeFileSync(i, n);
  } catch (r) {
    e.fail.warn(r);
  }e.log.writeln("File " + i.cyan + " created.");
};